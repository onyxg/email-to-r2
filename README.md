# email-to-r2
Cloudflare Email Router that parses the email and uploads it to a CloudFlare R2 Bucket, spilting attachments as separate files. Publishes event into Cloudflare Queues for downstream consumers
